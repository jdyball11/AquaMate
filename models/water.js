const mongoose = require('mongoose')

const WaterSchema = new mongoose.Schema({

    
})

// don't uncomment below as this will create new model in mongo.

// const Water = mongoose.model('Water', WaterSchema)

module.exports = Water